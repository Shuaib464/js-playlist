/* "for of"  loop 

NOte -> it iterate each element in array

*/

const arr = [1,2,3,4,5]

// for (const e of arr) {
//     console.log(e);
// }

const user = "shuaib ahamad";
for (const e of user) {
    console.log(`Each character is ${e}`);
}