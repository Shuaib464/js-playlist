// DOM (Document Object Model)

// getting attribute 
/*  
    document.getElementById('mainheading').getAttribute('class')

    // setting attribute -> .setAttribute('class', 'test')
*/

/* 
    1- .textContent -> gives the whole content including the content that is "hidden"
    2- .innerText -> gives the whole content except the hidden one

*/

/* 
        convert HTMLCollection to array
      -->  Array.from(html_collection-Name)

*/